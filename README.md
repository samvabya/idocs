## Installation

- Backend
```
cd backend
npm start
```
- Frontend
```
cd frontend
flutter run -d web-server --web-hostname 0.0.0.0 --web-port 8080 --wasm
```
also replace serverUrl in utils.dart to support mobile devices
